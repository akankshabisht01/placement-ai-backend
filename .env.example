## Copy this file to .env and fill in your values.

# MongoDB Atlas connection string.
# TIP: Use the exact connection string from the Atlas UI (Drivers > Python),
# which already URL-encodes the password. If your password has special
# characters and you type it manually, make sure it is URL-encoded.
# Examples of encoding: @ -> %40, # -> %23, & -> %26, / -> %2F
MONGODB_URI=mongodb+srv://<username>:<password>@<Cluster0>.<id>.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Database name to use (must exist or will be created on first write).
MONGODB_DB=placement_db

# If you still see auth errors, try adding '&authSource=admin' to MONGODB_URI
# when your user is created in the 'admin' database (common in Atlas):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority&authSource=admin

# Optional: Perplexity API key for LLM-based parsing (not required for rules)
# PERPLEXITY_API_KEY=your_perplexity_api_key_here
